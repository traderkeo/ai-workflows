<workflow-load-fix>
- Files: `src/components/SavedWorkflowsPanel.tsx`, `src/hooks/useFlowStore.ts`
- Plan:
  1. Address remaining dialog stacking + load issues and fix runtime node deletion error.
- Progress:
  - Reworked load flow per Option 2 (confirm while dialog open) and re-validated build; pending user retest.
</workflow-load-fix>
